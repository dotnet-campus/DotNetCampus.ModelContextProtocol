# Authorization

MCP Authorization lets the client and server authenticate and authorize a connection before or during protocol use.

DotNetCampus.ModelContextProtocol does not implement Authorization yet.
